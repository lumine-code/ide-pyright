{
  "name": "ide-pyright",
  "author": "lumine-code",
  "description": "Basedpyright language-server adapter for Python.",
  "repository": "https://github.com/lumine-code/ide-pyright",
  "bugs": {
    "url": "https://github.com/lumine-code/ide-pyright/issues"
  },
  "keywords": [
    "language-server",
    "lsp",
    "python",
    "intellisense",
    "type-checking"
  ],
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "lumine": "^1.0.0"
  },
  "main": "./lib/main",
  "files": [
    "lib",
    "spec"
  ],
  "scripts": {
    "test": "lumine --test spec",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "consumedServices": {
    "ide-client": {
      "versions": {
        "^1.0.0": "consumeIdeClient"
      }
    }
  },
  "dependencies": {
    "basedpyright": "1.39.9"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.2.2",
    "globals": "^17.9.0",
    "prettier": "^3.9.6"
  },
  "configSchema": {
    "serverPath": {
      "title": "Server Path",
      "description": "Absolute path to a Basedpyright `langserver` executable. Leave empty to use the copy the editor installed, or the server that ships with this package.",
      "type": "string",
      "default": ""
    },
    "features": {
      "title": "Features",
      "description": "Which parts of Basedpyright the editor uses. Turning one off hands it to the next server serving the same file, which is how you choose between Basedpyright and another Python server. Only what Basedpyright implements is listed: it has no formatter, code lens, or type hierarchy.",
      "type": "object",
      "properties": {
        "diagnostics": {
          "title": "Diagnostics",
          "description": "Report Basedpyright's type errors and warnings in the linter.",
          "type": "boolean",
          "default": true
        },
        "autocomplete": {
          "title": "Autocomplete",
          "description": "Offer Basedpyright's completions while typing.",
          "type": "boolean",
          "default": true
        },
        "hover": {
          "title": "Hover",
          "description": "Show Basedpyright's types and documentation on hover.",
          "type": "boolean",
          "default": true
        },
        "signature": {
          "title": "Signature Help",
          "description": "Show the call signature while typing arguments.",
          "type": "boolean",
          "default": true
        },
        "definition": {
          "title": "Go To Definition",
          "description": "Resolve definitions and declarations through Basedpyright.",
          "type": "boolean",
          "default": true
        },
        "references": {
          "title": "Find References",
          "description": "List occurrences of a symbol through Basedpyright.",
          "type": "boolean",
          "default": true
        },
        "callHierarchy": {
          "title": "Call Hierarchy",
          "description": "Serve incoming and outgoing calls to the hierarchy view.",
          "type": "boolean",
          "default": true
        },
        "symbols": {
          "title": "Symbols",
          "description": "Serve document and project symbols to go-to-symbol.",
          "type": "boolean",
          "default": true
        },
        "outline": {
          "title": "Outline",
          "description": "Fill the outline panel from Basedpyright's document symbols.",
          "type": "boolean",
          "default": true
        },
        "rename": {
          "title": "Rename",
          "description": "Rename symbols across the project through Basedpyright.",
          "type": "boolean",
          "default": true
        },
        "codeActions": {
          "title": "Code Actions",
          "description": "Offer Basedpyright's quick fixes in the intentions list.",
          "type": "boolean",
          "default": true
        },
        "inlayHints": {
          "title": "Inlay Hints",
          "description": "Show Basedpyright's inline type and parameter-name labels.",
          "type": "boolean",
          "default": true
        },
        "semanticTokens": {
          "title": "Semantic Tokens",
          "description": "Color identifiers with Basedpyright's semantic classification, where the editor has semantic highlighting enabled.",
          "type": "boolean",
          "default": true
        }
      }
    },
    "pythonPath": {
      "title": "Python Path",
      "description": "Path to the Python interpreter used for analysis. Leave empty for automatic discovery.",
      "type": "string",
      "default": ""
    },
    "venvPath": {
      "title": "Virtual Environment Path",
      "description": "Path to the folder that holds your virtual environments, not to one of them. Leave empty to let Basedpyright find the environment itself.",
      "type": "string",
      "default": ""
    },
    "analysis": {
      "title": "Analysis",
      "description": "What Basedpyright reads and how strictly it reads it. A `pyrightconfig.json` or the `[tool.pyright]` or `[tool.basedpyright]` table of `pyproject.toml` overrides these for the project that has one, and is the only place to override the severity of an individual rule.",
      "type": "object",
      "properties": {
        "typeCheckingMode": {
          "title": "Type Checking Mode",
          "description": "Strictness of Basedpyright's type checking. `recommended` and `all` are Basedpyright's own tiers beyond Pyright's.",
          "type": "string",
          "enum": [
            "off",
            "basic",
            "standard",
            "strict",
            "recommended",
            "all"
          ],
          "default": "basic"
        },
        "diagnosticMode": {
          "title": "Diagnostic Mode",
          "description": "Analyze only the open files or the whole workspace. Checking the workspace finds errors in files you have not opened, at the cost of a much longer first analysis.",
          "type": "string",
          "enum": [
            "openFilesOnly",
            "workspace"
          ],
          "default": "openFilesOnly"
        },
        "extraPaths": {
          "title": "Extra Paths",
          "description": "Additional directories to search for imports, for source layouts Basedpyright cannot infer — a `src` root, a vendored library, a generated stub tree. Relative paths resolve against the project root.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "stubPath": {
          "title": "Stub Path",
          "description": "Directory holding your own type stubs. Leave empty for the conventional `typings` directory.",
          "type": "string",
          "default": ""
        },
        "typeshedPaths": {
          "title": "Typeshed Paths",
          "description": "Where to look for typeshed's bundled stubs instead of the copy that ships with Basedpyright. Only the first entry is used.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "include": {
          "title": "Include",
          "description": "Paths to analyze. Leave empty to analyze the whole project root.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "exclude": {
          "title": "Exclude",
          "description": "Paths not to analyze at all, such as a build directory or a vendored tree.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "ignore": {
          "title": "Ignore",
          "description": "Paths that are still analyzed, because other files import them, but report no diagnostics of their own.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "autoSearchPaths": {
          "title": "Automatic Search Paths",
          "description": "Treat the conventional roots as import paths, so a `src` directory beside the project root resolves without listing it above.",
          "type": "boolean",
          "default": true
        },
        "useLibraryCodeForTypes": {
          "title": "Use Library Code For Types",
          "description": "Infer types by reading a library's source when it ships no stubs. Turning this off leaves untyped libraries as `Unknown`.",
          "type": "boolean",
          "default": true
        },
        "autoImportCompletions": {
          "title": "Auto-Import Completions",
          "description": "Offer symbols that are not imported yet and add the import when one is accepted.",
          "type": "boolean",
          "default": true
        },
        "logLevel": {
          "title": "Log Level",
          "description": "How much Basedpyright writes to its log, which `ide-client:show-log` opens.",
          "type": "string",
          "enum": [
            "Error",
            "Warning",
            "Information",
            "Trace"
          ],
          "default": "Information"
        }
      }
    }
  }
}
